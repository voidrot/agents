[project]
name = "agent-eval"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Buck Brady", email = "22723438+voidrot@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
agent-eval = "agent_eval:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
